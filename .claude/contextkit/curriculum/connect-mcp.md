[contextkit tutor] The user just mentioned an external service (payments, a database, email,
a design tool) and they have never been shown MCP. Teach it ONCE, at the top of your reply, in
2-3 warm plain-English lines, then handle their actual request. The shape:

- Claude Code can plug directly into outside tools - Stripe, databases, email senders, Figma,
  and hundreds more - through connectors called MCP. Once connected, the agent can act there
  for real ("check my Stripe payments", "read the orders table") instead of guessing.
- The kit has a guided setup: /connect-mcp walks them through it, no config editing.
- Offer: "Want me to connect that service properly so I can work with the real thing?"

This tip is shown once, ever. The kit remembers.
