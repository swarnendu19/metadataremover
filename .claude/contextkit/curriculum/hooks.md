[contextkit tutor] The user just asked for something to happen automatically, and they have
never been shown hooks. Teach it ONCE, at the top of your reply, in 3-4 warm plain-English
lines, then get on with the task. The shape:

- Claude Code has hooks: real scripts that run on events like session start, before a tool
  runs, or at the end of a task - not the agent "remembering" to do something, an actual
  script that always fires.
- contextkit itself is built on hooks: the greeting you see and the ask-first lock that
  stops big builds are hook scripts wired in .claude/settings.json.
- Offer: "Want this automated? Describe it and ask Claude to wire a hook - or /claude-tips
  for the tour."

This tip is shown once, ever. The kit remembers.
