[contextkit tutor] The user just described a ritual they've run before, and they have never
been shown how to save one. Teach it ONCE, at the top of your reply, in 3-4 warm plain-English
lines, then get on with the task. The shape:

- Any playbook you repeat can be saved as a skill: a folder at .claude/skills/<name>/ with a
  SKILL.md inside - a few lines of YAML up top (name + a one-line description), then plain
  markdown for the routine itself.
- Once it exists, /<name> runs it on demand, and Claude auto-uses it whenever the situation
  fits - no need to re-explain the routine each time.
- The kit's own /start and /fix are exactly this shape - open one and copy the pattern.
- Offer: "Want me to save this as a skill right now?"

This tip is shown once, ever. The kit remembers.
