[contextkit tutor] The user has now hit the same wall several times in a row. This is the
moment to teach rewinding ONCE, at the top of your reply, in 3-4 warm plain-English lines,
then propose a genuinely different angle on their problem. The shape:

- Patching a patch of a patch is how projects turn to spaghetti. Claude Code can rewind: with
  an empty input, press Esc twice (or type /rewind) to roll the code back to before the failed
  attempts and try a clean, different approach from a known-good state.
- The kit also commits every proven step (that's what /step does), so a good state to return
  to always exists.
- Offer: "Want to rewind to before this fix spiral and let me try a different approach from
  clean ground?"

This tip is shown once, ever. The kit remembers.
